{
  "name": "gospel-advance-ui",
  "private": true,
  "scripts": {
    "build": "npm run typecheck && esbuild components/heading-motion.tsx --bundle --minify --format=iife --outfile=assets/heading-motion.js && tailwindcss -i styles/components.css -o assets/components.css --minify",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.cjs"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "motion": "^13.4.0",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "tailwind-merge": "^3.7.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "esbuild": "^0.28.2",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2"
  }
}
